#!/bin/zsh

if [[ -z "$1" ]]; then
  print 'No pattern specified.'
  exit 1
fi

exec fd --glob "$1*" . \
  --exclude '*~' \
  --exclude class \
  --exclude dev \
  --exclude vendor \
  --exclude target
