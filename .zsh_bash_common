alias gl='git log --graph --pretty=format:"%Cred%h %C(bold blue)%an%C(yellow)%d%Creset %s %Cgreen(%cr) %Creset" --abbrev-commit --'

alias gs='git status'
alias ga='git add -A'
alias gam='git ls-files --modified | grep -v config | grep -v dispatch.* | grep -v launch | xargs git add'
alias gau='git ls-files --other --exclude-standard | xargs git add'
alias gb='git branch'
alias gc='git commit'
alias gd='git diff'
alias gdc='git diff --cached'
alias grmd='git ls-files --deleted | xargs git rm'
alias glf='git ls-files'
alias glfd='git ls-files --deleted'
alias pull='git pull --rebase origin master'
alias push='git push origin master'
alias pom='git push origin HEAD:master'
alias p311='git push origin HEAD:3.11.1'
alias gfo='git fetch origin'
alias gsh='git show "HEAD"'
alias gshn='git show "HEAD" --name-only'
alias gsh2='git show "HEAD^"'
alias g='git'
alias brc='emacs ~/.bashrc'
alias cbrc='cat ~/.bashrc'
alias gsp='git stash pop'
alias grom='git rebase origin/master'
alias gr311='git rebase origin/3.11.1'
alias grc='git rebase --continue'
alias gra='git rebase --abort'

alias add='git add'
alias ci='git commit'
alias co='git checkout'
alias gg='git grep'

alias e='emacs'
alias ec='emacsclient -t'
alias ed='emacs --daemon'
alias ep='ps aux | grep emacs'
alias dz='. ~/.zshrc'

alias amend='git commit --amend'
alias dbrc='. ~/.bashrc'
alias srp='g stash && grom && gsp'

export GIT_EDITOR='emacs --no-desktop'
export EDITOR=emacs

export PATH=$PATH:~/play-2.0
export PATH=$PATH:~/mongo/bin
export PATH=$PATH:~/hbase-0.92.1-cdh4.0.0/bin
export PATH=$PATH:~/bin
export PATH=$PATH:~/elasticsearch/bin

export BLEND_NAMESPACE=emarinelli_mbp

alias rmtilde='find . -name "*~" | xargs rm'
alias rmsvn='find . -name ".svn" | xargs rm -r'

alias srd='screen -r -d'
alias f='fg'

alias k1='kill -9 %1'
alias k2='kill -9 %2'
alias k3='kill -9 %3'
alias k4='kill -9 %4'
alias k5='kill -9 %5'
alias k6='kill -9 %6'

alias emacsnd='emacs --no-desktop'
alias end='emacs --no-desktop'

export SBT_OPTS='-Xmx512M -XX:PermSize=64M -XX:MaxPermSize=256M -XX:+UseConcMarkSweepGC -XX:+CMSClassUnloadingEnabled -XX:+CMSClassUnloadingEnabled -XX:-PrintCommandLineFlags' # -XX:+PrintGCDetails -verbosegc'

export ENSIME_JVM_ARGS=$SBT_OPTS

if [[ `uname` = Darwin ]]; then
   export JAVA_HOME=/System/Library/Frameworks/JavaVM.framework/Versions/CurrentJDK/Home
else
   export JAVA_HOME=/usr/lib/jvm/java-7-openjdk-amd64
fi

alias hbaselogs='tail -f `which hbase`/../../logs/hbase*.log'

alias dev='ssh emarinelli@eugene.dev.compose.cc'

[[ -s "$HOME/.rvm/scripts/rvm" ]] && . "$HOME/.rvm/scripts/rvm"

if [[ -f "$HOME/.amazon_keys" ]]; then
    source "$HOME/.amazon_keys";
fi

if [[ -r ~/.private ]]; then
    source ~/.private
fi
